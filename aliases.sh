# ls aliases
alias ll='ls -al'
alias la='ls -A'
alias l='ls -CF'

# git aliases
alias gs='git status'
alias gr='git remote -v'
alias gl='git log'
alias gc='git commit'

# editor (update path to your nvim)
alias v='nvim'
