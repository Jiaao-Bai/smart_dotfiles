# smart_dotfiles

生存装备库。

## 装备清单

### Neovim 配置
- **位置**: `.config/nvim`
- **安装 (覆盖模式)**:
  ```bash
  mkdir -p ~/.config
  cp -rv .config/nvim ~/.config/
  ```

### Shell 配置 (.zshrc)
- **安装 (覆盖模式)**:
  ```bash
  cp .zshrc ~/.zshrc
  ```
