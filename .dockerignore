# 构建上下文过滤。
# 注意 1：故意「不」忽略 .git —— 镜像里需要它来支持 `git pull` 更新 dotfiles。
# 注意 2：不要忽略任何「被 git 跟踪」的文件，否则容器内工作树会与 .git 不一致，
#         git 会把它们视为「已删除」，导致 git pull 前工作树脏。
#         这里只排除 git 本就不跟踪的 OS/编辑器杂物。

.DS_Store
*.swp
*.swo
*~
